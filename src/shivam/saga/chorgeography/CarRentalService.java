package shivam.saga.chorgeography;

public class CarRentalService {
//    @EventListener
//    public void onHotelBooked(HotelBookedEvent event) {
//        try {
//            // Attempt to book a car
//            bookCar(event.getBookingRequest());
//            eventBus.publish(new CarBookedEvent(event.getBookingRequest()));
//        } catch (Exception e) {
//            eventBus.publish(new BookingFailedEvent(event.getBookingRequest()));
//        }
//    }
}
