package shivam.saga.chorgeography;

public class HotelService {
//    @EventListener
//    public void onFlightBooked(FlightBookedEvent event) {
//        try {
//            // Attempt to book a hotel
//            bookHotel(event.getBookingRequest());
//            eventBus.publish(new HotelBookedEvent(event.getBookingRequest()));
//        } catch (Exception e) {
//            eventBus.publish(new BookingFailedEvent(event.getBookingRequest()));
//        }
//    }
}
