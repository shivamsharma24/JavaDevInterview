package shivam.saga.chorgeography;

public class FlightService {
//    @EventListener
//    public void onBookingInitiated(BookingInitiatedEvent event) {
//        try {
//            // Attempt to book a flight
//            bookFlight(event.getBookingRequest());
//            eventBus.publish(new FlightBookedEvent(event.getBookingRequest()));
//        } catch (Exception e) {
//            eventBus.publish(new BookingFailedEvent(event.getBookingRequest()));
//        }
//    }
}
